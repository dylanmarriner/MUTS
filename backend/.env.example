# Database
# SQLite (default - no setup required)
DATABASE_URL="file:./dev.db"

# PostgreSQL (optional - requires Docker/local Postgres)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/muts_db"

# Server
NODE_ENV="development"
HOST="0.0.0.0"
PORT=3000

# Security
JWT_SECRET="your-secret-key-here"

# Logging
LOG_LEVEL="info"

# Operator Mode (CRITICAL: Determines system capabilities)
# Options: dev (default), workshop, lab
# dev: Development with mock interface, no ECU writes
# workshop: Real vehicle servicing, ECU writes allowed
# lab: Research mode with expanded capabilities
OPERATOR_MODE="dev"